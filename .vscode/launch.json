{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/lab1/buggy_programs/bounds",  // Ensure this is the correct path to your executable
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",  // Optional, only if you have a build task
            
        }
    ]
}
